# Complete Action

1. Reset current-feature.md first (before committing anything):
   - Change H1 back to `# Current Feature`
   - Clear Goals and Notes sections (keep placeholder comments)
   - Add feature summary to the END of History
   - Set Status back to "Not Started"
2. Stage ALL changes (feature files + the reset current-feature.md) and make ONE commit with a descriptive message
3. Switch to main and merge the feature branch (fast-forward)
4. Delete the local feature branch
5. Push main to origin once
6. If feature branch was previously pushed to origin, delete it from origin
